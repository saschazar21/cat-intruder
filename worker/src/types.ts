export const CAT_PREFIX = 'cat:';
export const DETECTION_PREFIX = 'detected:';
export const DIRECTION_PREFIX = 'direction:';

export enum DETECTION {
  YES = 'yes',
  NO = 'no',
}

export enum DIRECTION {
  INSIDE = 'inside',
  OUTSIDE = 'outside',
}
